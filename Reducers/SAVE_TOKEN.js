import * as actionTypes from "../Actions/SaveToken";

const intialState = {
    isLoading:false,
    hasError:false,
    token:""
}

const SAVE_TOKENS = (state = intialState , action) =>{
    switch(action.type){
        case actionTypes.
        default :
        return state
    }
 }

export  default SAVE_TOKENS